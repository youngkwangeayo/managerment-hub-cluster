clusterName: cluster-b-dev
# argocd cluster add <context> --name cluster-b 후 나온 API 서버 URL로 교체
destinationServer: https://CLUSTER_B_API_SERVER
targetRevision: main

cascadeDelete: true

syncPolicy:
  automated:
    prune: true
    selfHeal: true

apps: []
  # 예시: cluster-b에 배포할 앱 추가
  # - name: my-service
  #   path: helm-charts/my-service
  #   namespace: dev-my-service
  #   valueFiles:
  #     - values.yaml
  #     - values-dev.yaml
